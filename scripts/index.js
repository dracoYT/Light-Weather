console.log(`
Air fetch Cosole is not avalible\nWait 1 week more to use it, it still in development`);
var data = InputEvent;
function fetch() {
    if (fetch.data = arduino) {
        
    }
}